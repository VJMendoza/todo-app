import { ElevateDirective } from './elevate.directive';

describe('ElevateDirective', () => {
  it('should create an instance', () => {
    const directive = new ElevateDirective();
    expect(directive).toBeTruthy();
  });
});
